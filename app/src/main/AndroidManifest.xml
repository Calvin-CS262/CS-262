<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jc56.cs262.calvin.edu.caluberprototype">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BasketActivity"
            android:label="@string/title_activity_basket"
            android:parentActivityName=".HomePage"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".UserProfile"
            android:parentActivityName=".HomePage"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".CreateRide"
            android:parentActivityName=".HomePage"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".JoinRide"
            android:parentActivityName=".HomePage"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".HomePage" />
        <activity
            android:name=".Signup"
            android:parentActivityName=".HomePage"></activity>
        <activity
            android:name=".TabHomePage"
            android:label="@string/title_activity_tab_home_page"></activity>
    </application>

</manifest>